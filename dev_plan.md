# 프로젝트 개요
2D 탑다운 픽셀 아트 스타일의 좀비 서바이벌 액션 게임을 개발하려고 해. 내가 디렉팅을 할 테니, 너는 내 지시에 맞춰 코드를 작성하고 구조를 잡아줘.

# 기술 스택
* 언어: Python
* 라이브러리: Pygame-CE (성능 및 최신 기능 활용)
* 아키텍처: 확장성을 고려한 모듈화된 객체 지향 구조 (엔티티, 맵, 무기, 매니저 클래스 분리)

# 게임 핵심 기획서
1. 맵 및 환경
   * 랜덤 생성 테마 (지하동굴, 폐허도시, 정글, 눈)
   * 플레이어가 숨을 수 있는 '집' 또는 '은폐 구역' 존재

2. 엔티티 (Entities)
   * 플레이어: 이동, 무기 발사, 은폐 가능
   * 일반인 (NPC): 무작위 배회. 좀비에게 피격 시 수십 초 내로 좀비로 변이.
   * 좀비 (4종): 일반 좀비, 스피드 좀비, 거인 좀비, 4족 보행 좀비. 플레이어 및 일반인 추적.

3. 무기 및 성장 시스템
   * 무기류: 권총, 장총, 활(화살), 샷건, 화염방사기, 머신건
   * 업그레이드: 맵에 드랍되는 '나사' 아이템을 획득하여 연사력, 재장전 속도 등 개조.

# 첫 번째 마일스톤 (Phase 1) 지시사항
우선 게임의 가장 핵심이 되는 **'감염 시스템' 프로토타입**을 먼저 만들자. 
다음 기능이 포함된 기본 스크립트를 작성해 줘:
1. Pygame-CE 창 초기화 및 기본 게임 루프 설정.
2. 플레이어 캐릭터(단순한 도형) 이동 구현 (WASD).
3. 무작위로 돌아다니는 일반인(NPC) 여러 명과, 최초의 '일반 좀비' 1마리 스폰.
4. 좀비가 일반인과 충돌하면 타이머가 작동하고, 일정 시간 뒤 일반인이 좀비로 변해 플레이어를 쫓아오는 로직 구현.

# Phase 2: 은폐 및 소리 기반 감지 시스템(Stealth & Detection) 구현

Phase 1에서 구현된 기본 이동 및 감염 시스템에 이어, 플레이어의 생존을 위한 스텔스 메커니즘을 추가하려고 해. 아래의 세부 로직을 구현해 줘.

1. 은폐 구역(Hiding Zones) 추가
   * 맵에 '풀숲'이나 '집(건물 내부)' 역할을 하는 특정 타일 또는 구역(Rect)을 배치해 줘.
   * 플레이어가 이 구역 안에 들어가면 `is_hidden = True` 상태가 되어, 좀비의 시야에서 벗어남.

2. 소리 방출(Noise Emission) 시스템
   * 플레이어 주변에 '소리 반경(Noise Radius)' 개념을 추가해 줘. 
   * 가만히 서 있거나 은폐 중일 때는 소리 반경이 0.
   * 이동할 때(발걸음)는 소리 반경이 일정 크기로 커짐. (달리기/걷기 구분이 있다면 반경 크기를 다르게 설정)

3. 좀비 AI(상태 머신) 개선
   * **배회 (Wander):** 타겟이 없을 때 무작위로 걸어다님.
   * **감지 (Investigate):** 플레이어의 이동 소리(Noise Radius)가 좀비의 인지 범위에 닿으면, 그 소리가 발생한 '마지막 위치(Last Known Position)'로 이동함.
   * **추적 (Chase):** 플레이어가 은폐하지 않은 상태(`is_hidden = False`)로 좀비의 시야 범위 내에 들어오면 플레이어를 직접 추적함. 은폐 구역으로 도망가서 시야에서 사라지면 다시 '감지' 또는 '배회' 상태로 돌아감.

코드를 작성할 때 소리 반경이나 시야 범위를 디버깅하기 쉽도록, 화면에 반투명한 원이나 선(Debug Draw)을 그려서 감지 로직이 잘 작동하는지 눈으로 확인할 수 있게 해줘.

# Phase 3: 다양한 지형지물 및 은신처 기반 파밍(Loot) 시스템 구현

이제 맵을 더 다채롭게 만들고, 플레이어가 은신처를 탐험해야만 하는 동기를 부여하는 파밍 시스템을 추가할 거야. 확장성과 객체 지향 구조를 신경 써서 구현해 줘.

1. 지형지물(Terrain) 클래스 확장
   * 맵을 구성하는 다양한 환경 요소를 추가하고, 각각 다른 속성(Collision, Stealth, Speed 등)을 부여해.
   * **은신처 (Hideouts):** 동굴(Caves), 빈 집 등. (내부에 진입하면 `is_hidden = True`가 됨)
   * **장애물/환경 지형:** 호수(Lakes - 이동 불가 또는 이속 감소), 골짜기(Valleys - 시야는 통과하지만 이동 동선이 제한됨).

2. 무기 및 아이템 클래스 설계
   * **무기(Weapon) 객체:** 권총, 샷건, 머신건 등 (이름, 연사력, 재장전 속도 등의 속성 포함).
   * **재화(Resource) 객체:** 무기 업그레이드용 '나사(Screw)'.
   * **아이템 매니저(Item Manager):** 아이템의 스폰과 관리를 담당.

3. 특정 구역(은신처) 내 아이템 스폰 로직
   * 맵이 생성될 때, 아이템(무기와 나사)은 반드시 **'은신처(동굴, 집 등)로 지정된 Rect 구역 내부'**에서만 무작위 좌표로 스폰되게 만들어 줘. 길바닥에는 스폰되지 않음.
   * 플레이어가 은신처에 들어가서 해당 아이템과 충돌하면 인벤토리에 획득/장착하는 로직을 구현해 줘. 

(디버그용으로 은신처 구역의 테두리 색상을 다르게 칠하고, 내부에 스폰된 무기/나사는 구별되는 색상의 작은 도형으로 표시해 줘.)


Phase 6: 대규모 오픈월드(GTA급) 및 동적 청크 스트리밍(Dynamic Chunk Streaming) 시스템 구현

기존의 제한된 맵 크기(2400x1800)를 대폭 확장하여, 거의 무한하거나 거대한 월드(예: 50,000 x 50,000 이상)를 탐험할 수 있는 시스템을 구축할 거야. 웹 브라우저의 메모리 한계와 프레임 드랍을 방지하기 위해 플레이어 주변만 동적으로 로드하고 해제하는 최적화 기법을 적용해 줘.

1. 그리드 청크(Grid Chunk) 기반 맵 관리

청크 시스템 도입:

전체 월드를 일정 크기의 격자(예: 1000x1000 픽셀 단위의 청크)로 분할하여 관리해 줘.

플레이어가 위치한 청크를 중심으로 주변 9개(3x3) 혹은 25개(5x5) 청크만 활성화(Active)하고, 이 범위를 벗어난 청크는 비활성화(Unload)하거나 메모리에서 해제해야 해.

지형지물 및 은신처 동적 생성:

플레이어가 새로운 청크 영역으로 이동할 때, 해당 청크의 테마(지형지물, 동굴, 빌딩 등)가 절차적 알고리즘(Procedural Generation / 시드 기반 난수)에 의해 동적으로 생성되게 해 줘.

한 번 생성되었다가 비활성화된 청크는 플레이어가 다시 돌아왔을 때 이전 상태(예: 파괴된 장애물, 드랍된 아이템 등)를 복구할 수 있도록 상태를 저장/직렬화하는 가벼운 캐싱 메커니즘을 구현해 줘.

2. 동적 객체 스트리밍 (Dynamic Spawning & Despawning)

NPC 및 좀비 풀링/스트리밍:

활성화 반경(Active Radius): 플레이어 기준 약 1500px 이내의 영역만 엔티티(일반인, 좀비)가 실제로 행동하고 물리 연산을 수행함.

스폰 반경(Spawn Margin): 플레이어의 시야 밖(약 1000px ~ 1500px 사이)에서 새로운 인간 NPC나 좀비가 동적으로 스폰됨.

데스폰 반경(Despawn Radius): 플레이어와의 거리가 1800px 이상으로 멀어진 엔티티(좀비, 일반인, 파티클, 아이템 등)는 즉시 메모리 절약을 위해 비활성화(제거 또는 객체 풀로 반환) 처리함.

감염 데이터 보존:

멀리 떨어져서 비활성화된 인간 NPC들의 감염 타이머나 좀비들의 상태는 단순화된 시뮬레이션(Abstracted Simulation) 데이터로만 가지고 있다가, 플레이어가 다시 가까이 가면 그 상태에 맞춰 실시간으로 다시 생성(Spawn)되도록 유기적으로 연결해 줘.

3. 성능 최적화 (Spatial Partitioning & Culling)

쿼드트리(Quadtree) 또는 그리드 해시 맵 사용:

수백 개의 엔티티와 총알 간의 충돌 연산을 할 때 O(N^2) 루프를 돌면 성능이 저하되므로, 플레이어 근처의 엔티티들만 필터링하여 충돌 검사를 할 수 있도록 공간 분할(Spatial Partitioning) 인덱스를 구현해 줘.

렌더링 컬링(Frustum Culling):

화면 카메라 범위를 벗어난 오브젝트(Draw) 연산은 아예 캔버스 그리기를 생략(Culling)하여 렌더링 오버헤드를 최소화해 줘.

4. 플레이어 탐험 HUD 추가

플레이어가 아주 방대한 맵을 탐험하므로 아래 정보가 실시간으로 기록되는 HUD를 추가해 줘:

플레이어의 절대 좌표 (X, Y)

현재 활성화된 청크 개수 및 메모리에 로드된 총 엔티